run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gofmt
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - gosec

linters-settings:
  gosec:
    excludes:
      - G104 # we use errcheck for unhandled errors
