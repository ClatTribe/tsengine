import { ComingSoon } from "@/components/ui/coming-soon";
export default function Page() {
  return <ComingSoon title="Inbox" phase="Phase 2" blurb="Keyboard-driven approval queue — approve/reject the agent's proposed fixes through the gated desk." />;
}
