import { ComingSoon } from "@/components/ui/coming-soon";
export default function Page() {
  return <ComingSoon title="Assets & connections" phase="Phase 6" blurb="Connect a system (OAuth), see monitored assets + last-scan, and trigger a scan now." />;
}
