# Binaries
bin/
/tsengine
/tool-server
*.exe
*.dylib
*.so

# Coverage / profiling
*.out
coverage.html
*.prof

# Editor / local agent state
.idea/
.vscode/
.claude/
*.swp
.DS_Store

# Runtime artifacts
runs/
scan_runs/
*.log

# Generated OSINT threat-intel corpus (cron-refreshed, not committed — scans
# pin it via TSENGINE_THREAT_INTEL_CORPUS; the embedded snapshot is the
# checked-in fallback). Leading slash anchors to repo root so it does NOT
# match internal/corpus/ source.
/corpus/

# Secrets / keys (never commit)
*.pem
*.key
attestation-private.bin
.claude/launch.json

# compiled platform binary (built locally; never commit)
/platform
